# Deployment Configuration
# Copy this file to .env.agent and replace with your actual values

# ECR Repository Name
# This should match the repository name in your Terraform configuration
# Example: agentcore-test-runtime-agent-code-ecr-repository"
ECR_REPOSITORY_NAME=agentcore-test-runtime-agent-code-ecr-repository"

